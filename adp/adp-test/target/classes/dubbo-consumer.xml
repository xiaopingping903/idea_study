<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
      http://www.springframework.org/schema/beans/spring-beans.xsd
      http://code.alibabatech.com/schema/dubbo
      http://code.alibabatech.com/schema/dubbo/dubbo.xsd
      ">

    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
    <dubbo:application name="adp-dubbo-test-consumer" />

    <!-- 使用zookeeper广播注册中心暴露发现服务地址  -->
    <!--测试环境-->
    <dubbo:registry address="zookeeper://localhost:2181" />
    <!--正式环境-->
    <!--<dubbo:registry  address="zookeeper://10.138.8.223:2181" />-->

    <!-- 生成远程服务代理，可以和本地bean一样使用configReadService -->
    <dubbo:reference  id="kpiService" interface="com.haier.adp.kpi.service.KpiService"
                      version="1.0" check="false" />

</beans>